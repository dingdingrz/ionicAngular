

# Angular
类编程，通过this拿取属性 单项数据流
## Angular/core
底层核心库，存放在框架基础运行机制，装饰器，生命周期，依赖注入核心能力
- 模块装饰器 ngModule 负责管理组件，声明组件，
    1. 类装饰器
    2. declaretions 数组 注册组件
    3. imports 外部依赖
    4. exports 数组 共享模块，根组件用不到，没有可共享的
    5. providers 
    6. bootstrap 根组件 启动引导组件

- 生命周期
   1. ngOninit 初始化钩子
   2. ngOnchanges 输入属性@input 发生变化

- 依赖注入
    1. Injector 
    2. construction 在构造函数进行实例化 private public protect
    3. public 在一个组件定义cart.name = 2 在另外一个组件可直接通过类cart.name 获取 protect 外部访问不了，子类继承
    - 父子传值 子组件通过@Input() a!:string  ! 非空断言 由父组件传入，初始化不会为空，
        1. @Output a = new EventEmitter() 外抛事件，子组件传值，父组件通过在函数中接收$event 来接收传递过来的值 @output(test) ga = new EventEmitter() 定义ga 是个事件，test为ga的别名，
        2. 共享service + rxjs 多层嵌套，类似与vue中的vuex
- DOM操作
    1. ElementRef
- 内置指令 ngIf
## Angular/forms
表单核心库，FormsModule 实现双向绑定 需要引入 
- NgForm  内置表单校验器
ReactiveFormsModule 响应式表单， FormControl 复杂表单 const test = new FormControl('')
FormGroup
- Validators 校验对象，有validators.required
- formBuilder 表单快捷建造工具，响应式表单，fb.group 创建整份表单，fb.array 动态增减控件
    1. setValue({}) 更新表单中的所有
    2. patchValue({}) 更新单个字段
    3. get('字段名') 拿到单个FromControl 控件
    4. valueChange(callback) 监听数据流
## Angular/common

  - location  路径相关，location.back()回退 控制浏览器的相关行为
  - 页面渲染相关指令 ngIf ngFor 在这个库里面，不引入模板不认识这个指令
  - 属性绑定指令 [ngClass]
  - 内置管道 {{test | data:'yyyy'}}
  - HttpClientModule 请求模块
    1. http.get(url)
## angular/router
    - Router 全局路由操控，跳转，监听整个路由事件
        1.this.router.navigate(['/home'，'id'])
    - ActivateRoute 当前路由信息，获取路径传参
        1. this.route.snapshot.params['id']
    - 路由跳转通过routerLink  模板声明方式跳转，编程式跳转
    1. 声明式跳转  无需注入服务，简介，自带高亮
        - routeLink  routeLinkActive 路由激活高亮
        - 跳转时保留参数 <a routeLink='/test' [testParams]='test'>  /test?testParams=test
    2. 编程式跳转 
        - 基础跳转 this.route.navigate(['test'])
        - 携带动态路径参数 this.route.navagire(['test'],id) /test/:id
        - 携带queryParams this.route.navigate(['test'],{
            queryParams:{page:1,pageSize:10}
        })
    
    3. 原生跳转 
    4. navigateByurl() 接收完整的字符串url
    5. 组件内不修改路由，采用snape 获取参数，组件内会切换参数采用订阅数据流的字段，可实时监听
## angular 生命周期
    - ngOnInit 初始化
    - ngONDestory 销毁
    - ngAfterContentInit dom 渲染完毕，可操作dom
## Rxjs
    - Observable 生成可观察的对象
    - subscripe 订阅，不订阅观察对象的函数不会执行，订阅的操作
    - pipe 管道，对对象进行操作处理
    - subject 广播
    - of     
# ionic
 移动端开发库
 - ionic serve
 - ionic build
 - ionic cap add android 项目新增Android文件夹，生成壳子
 - ionic cap sync  把ionic build的成果物同步到壳子里面
 - ionic cap open android    gradle 生成的android 进行打包，生成apk

 # Capacitor
 可生成一个app壳子，把ionic 打包的资源丢进壳子里面 gradle 把这个壳子打包成apk
- capacitor/core
- capacitor/cli
# Capacitor/camera
顶层大类camera getphoto()拍照选图
返回的对象结构{
    webPath 页面可直接展示
    base64String
    dataUrl
    format
    exif 照片相关信息 
}
- checkPermissions()
- requestPermissions
# svgjs
- container element 
    1. group 逻辑分组 图层概念，可整体变换，内部图形完全继承父属性，可无限嵌套
        - g.add(内容) 把内容添加到分组 g.children() g.first() g.last()
        - 分组层级调整，g.back 放在底层，g.front() g.forward() g.backward
    2. symbol 摸具，不可见的模型模板 具有独立坐标系 复用性能高
    3. defs 仓库，页面不可见，存放在里面的属性和方法可多次被复用 ，只能用在资源定义，不能放展示的图形

```javascript
 const house = draw.defs().symbol().viewBox(0,0,100,100)
 draw.use(house)
 draw.use(house)
 //多个复用

``` 
4. link 给盒子增加跳转
5. rect circle radius
# sqlite
## 不同表的连接方式有五种
- inner join  内连接，匹配两个表中有数据的表 ，两边都有留下，
- left join 左连接，左边表格能匹配上展示数据，匹配不到，展示null 左边全留下
- right join
- full outer join 全外连接，两边内容全留下
- cross join 交叉连接，左边和右边进行交叉配对
## group by 
group by 的作用是对数据进行分组，对每条分组配置count 进行计算
having 对分组进行在过滤
in() 条件

## 表的查询方式
- 子查询 一条查询语句嵌套另外一个查询语句 select 字段名 from 表名 where 条件 in  select 字段 from 表名
- 合并查询 select 字段名 from 表名 union select 字段名 from 表名   union 自动去重 ，union all 不去重
-先找表在连表，where 筛选单行，group 来分组，having 来筛组，选字段排顺序，最后limit 截条数